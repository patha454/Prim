name: CI
on: push

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: install
      run: apt install clang-tidy
    - name: clang-tidy
      run: find . -name "*.c" -or -name "*.h" | xargs clang-tidy --checks=*
